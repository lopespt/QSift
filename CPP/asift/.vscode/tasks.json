{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Compile ASIFT",
            "type": "shell",
            "command": "pwd && cmake .. && make -j8",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },
        {
            "label": "Run ASIFT",
            "type": "shell",
            "command": "${workspaceFolder}/build/OrdenacaoASift",
            "problemMatcher": [],
            "dependsOn": ["Run Compile ASIFT"]
        }
    ]
}